import 'package:flutter/widgets.dart';

class Dimensions {
  static EdgeInsets get pagePadding => const EdgeInsets.all(16);
  static EdgeInsets get pagePaddingHorizontal => const EdgeInsets.symmetric(horizontal: 16);
  static EdgeInsets get pagePaddingVertical => const EdgeInsets.symmetric(vertical: 16);
}
